#!/bin/bash

file=/opt/flutter/bin/cache/lockfile
uname2="$(stat --format '%U' "$file")"
if [ "x${uname2}" = "x${USER}" ]; then
	    echo $USER is owner of $file
    else
	    echo $USER is not owner of $file
	    sudo chown ${USER}:wheel $file
fi

sh /opt/android-studio/bin/studio.sh

